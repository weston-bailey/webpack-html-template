{
  "name": "canvas-art-webpack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack build",
    "serve": "browser-sync start --server --serveStatic ./dist",
    "dev": "npm run build && npm run serve && nodemon --exec npm run build && browser-sync reload",
    "open": "npm run dev && open -a 'Google Chrome' ./dist/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.5",
    "css-loader": "^6.3.0",
    "style-loader": "^3.3.0",
    "webpack": "^5.53.0",
    "webpack-cli": "^4.8.0",
    "webpack-html-plugin": "^0.1.1"
  },
  "dependencies": {
    "html-webpack-plugin": "^5.3.2"
  }
}
